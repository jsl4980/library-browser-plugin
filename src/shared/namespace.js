(function bootstrapNamespace(globalScope) {
  const root = globalScope || self;

  if (!root.LibraryBrowser) {
    root.LibraryBrowser = {};
  }
})(typeof globalThis !== "undefined" ? globalThis : self);
